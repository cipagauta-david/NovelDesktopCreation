/**
 * @deprecated Use `SectionCard` directly. This alias exists only for
 * backwards compatibility and will be removed in a future refactor.
 */
export { SectionCard as PanelSection } from './SectionCard'
export type { SectionCardProps as PanelSectionProps } from './SectionCard'
